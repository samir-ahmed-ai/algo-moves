# Archived scripts

One-off migration and repair tooling kept for reference. Active scripts live in the parent `scripts/` folder and are listed in `package.json`.

To run an archived script:

```bash
node scripts/archive/repair-quiz-labels.mjs
```
