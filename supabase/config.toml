# Supabase local config for CLI migrations (link with: supabase link --project-ref <ref>)
project_id = "algo-moves-arcade"

[db]
major_version = 15

[auth]
enabled = true

[auth.external.anonymous_users]
enabled = true
