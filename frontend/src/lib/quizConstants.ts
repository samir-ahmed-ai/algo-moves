/** Shared quiz timing — mobile deck and Code Studio use the same rhythm. */
export const QUIZ_CORRECT_MS = 850;
export const QUIZ_WRONG_MS = 1900;

/** Reorder multiple-choice options on display (all quiz surfaces default to on). */
export const QUIZ_SHUFFLE_BY_DEFAULT = true;
