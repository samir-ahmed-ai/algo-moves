/**
 * Plugin viz typography — mirrors canvas `--node-fs*` tokens. Definitions live
 * in the design token barrel; re-exported here as the plugin-facing
 * entry point (`import { vizText } from '@/plugins/_shared/vizTokens'`).
 */
export { vizText, vizPad } from '@/design/tokens';
